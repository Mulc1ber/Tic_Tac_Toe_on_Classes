import { Game } from './Components';

export const App = () => {
    return (
        <div className="w-full h-full bg-gradient-to-r from-sky-200 to-pink-200">
            <Game />
        </div>
    );
};
